1.必须return 0
2.不要用gets(),用fgets()或cin.geetline()
C++读入字符串：std::string s;getline(cin,s);只适用字符串
读入字符数组：char s[100];cin.getline(s,100);只适用字符数组
3.用freopen进行本地测试：
    在代码的同一文件夹下新建in.txt
    将测试用例输入in.txt
    代码第一行输入freopen("in.txt","r",stdin);freopen("out.txt","w",stdout);
4.千万注意int 和double计算的时候乘1.0
5.实数用double，千万别用int
6.整数向上取整公式：s/v除不尽：int time=(s+v-1)/v+10;
7.printf("02d:%02d\n",h,m);自动补0
8.switch
9.注意除0的坑
10.两边之和大于第三边才能构成三角形，平方和 > 最大边平方 → 锐角，arr[0]==arr[1] && arr[1]==arr[2]
11.int w = x + i;
        int week = (w - 1) % 7 + 1;
12.stoi只适合字符串转数字，字符转数字要用s[i]-'0'
13.string::npos
14。千万记得初始化int变量
15.绝对值：对int 用abs(),对long long用llabs(),对float和double用fabs()
16.分清楚情况，不满如何，满了如何
17注意字符串内置函数的参数
18.测试用例，0和1
19.涉及n-1,n+1注意边界
20.握手问题
21.C语言里字符串末尾必须要加\0,字符串长度=size()+1;
22.int t=i;注意不要修改for的i变量
23.一种情况只想处理一次，不想放在while的条件里，用do-while
24.unordered_set的去重
25.也不一定非得用unordered_set，想想数组
26.STL的容器，迭代器都能用，*s.begin()
27.vector<pair<char,int>> 排序会按字母顺序排，不是按出现次数
28.else if(int idx=s.find('.'))，idx定义在条件里，后面代码块里不能用
29.fixed+setprecision强制输出，setprecision最多输出
30.注意数组和字符串erase的参数不一样，字符串是下标，数组是迭代器
v.erase(v.begin());    // 删除第一个元素
v.erase(v.begin() + 2); // 删除第 3 个元素（下标 2）
31.学会在循环输入的过程中处理这组数据
32.max是关键字，不能设置int max
33.先反转再去0,if((idx=s.find('.')) != string::npos){
        string zhengshu=s.substr(0,idx);
        string xiaoshu=s.substr(idx+1);

        reverse(zhengshu.begin(),zhengshu.end());
        reverse(xiaoshu.begin(),xiaoshu.end());

        while(zhengshu.size()>1 && zhengshu[0]=='0') zhengshu.erase(zhengshu.begin());
        while(xiaoshu.size()>1 && xiaoshu.back()=='0') xiaoshu.pop_back();
34.结构体可直接赋值
35.纯 C 语言 → 必须加 struct
C++ 语言 → 可以不加 struct
用了 typedef → C 和 C++ 都不用加
35.求集合的所有子集和的和：一个集合有 n 个元素每个元素在所有子集中出现的次数 = 2^(n-1)
36.找多个答案中的最小答案时，刚开始就从最小开始试
 for (int num = 4; num <= n; num += 2) {
        // 找第一个最小的质数 a，使得 num - a 也是质数
        for (int a = 2; a <= num / 2; a++) {
            int b = num - a;
            if (isPrime(a) && isPrime(b)) {
                // 找到就输出，立刻退出，保证第一个最小
                cout << num << "=" << a << "+" << b << endl;
                break;
            }
        }
    }